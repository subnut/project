a=input("Enter the string: ")
b=input("Enter the substring to find: ")
c=int(input("Enter the occurence whose index is to be found: "))
d=len(b)
for i in range(c):
    x=a.find(b)
    a=a[x+d:]
if x==-1:
    print("The substring couldn't be found in the given string as many times as the occurence required")
else:
    print("The required occurence starts from the index",x)
